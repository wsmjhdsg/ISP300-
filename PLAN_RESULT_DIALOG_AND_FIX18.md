# 方案文档：烧录结果弹窗监控 + 控件[18]修复（仅方案，未改任何代码）

> 2026-09-08 ｜ 状态：待确认后实施
> 依据：`logs/auto_isp.log` 17:06~17:07 真机执行日志 + 用户提供的成功弹窗截图

---

## ✅ 实施状态（17:50 用户确认"开干"后已全部落地 v3.5）

| 优先级 | 内容 | 状态 |
|--------|------|------|
| P0 | ListItem 纯数字标题改行号定位 + 失败 dump 行列表 | ✅ `core/isp_controls.py` |
| P0 | 步骤失败即中止（防烧错）+ UI「失败即中止」勾选(默认开) | ✅ `core/simulator.py` / `ui/main_window.py` |
| P1 | 新增 `waitresult` 步骤类型（弹窗监控判定烧录结果 + 自动关窗） | ✅ `core/result_dialog.py` + 全链路接入 |
| P2 | 步骤级"失败后继续"勾选项 | ⏸ 未做（可由全局开关替代，确有需要再加） |

- 判定策略按用户确认：**只识别 `COMPLETE`/`Verify OK` 成功弹窗，超时未收到一律判失败**；"失败即中止"默认开启。
- 测试 57 个用例全绿；README 已同步 v3.5；待重新打包 exe 真机验证。

---

## 一、日志证据：[18]/[6] 失败的真正原因

```
[17:06:58] 中文输入法已临时关闭(英文模式)
[17:06:59] Launched program: ISP300Host_ver5.lnk
[17:07:04] 已点击控件: Button "Read I.S.P"（第 1 次成功）
[17:07:05] 已点击控件: Button "LOAD"（第 1 次成功）
[17:07:17] 点击第 1/3 次失败(未找到控件(可能尚未加载完成))
[17:07:19] 点击第 2/3 次失败(未找到控件)
[17:07:21] 最终失败(已重试 3 次) type='ListItem' title=6 : 未找到控件
[17:07:22] 已点击控件: Button "Send Data"   ← 失败后仍继续执行
```

**结论：不是"最小化离屏"问题**（那个修复本身有效，本机已验证），而是 **ListItem 按标题精确匹配失败**。

### 根因分析

你在 17:05~17:06 录制的控件点中，空的槽位行标题就是纯数字（如 `4`、`5`、`6`），而有设备的行标题是一长串文本（如 ` 1 R5F5634E 15.9744 20011305mot [2048] ASYNC`，注意行首还有空格）。

执行时执行 `Read I.S.P` 后**列表被刷新**，导致：

1. **标题内容变化**：录制时是空槽 `6`，执行时该行可能已有设备（标题变长串），或反之；
2. **精确匹配过于脆弱**：当前匹配是"控件类型 + 标题完全相等"，多一个空格、行首空格、数字变文本都会失配；
3. **可能的滚动问题**：ListView 若只暴露可见行，[18] 在第 18 行可能滚动到视口外而枚举不到。

### 附带发现的严重隐患（建议一并处理）

**步骤失败后引擎继续执行后续步骤**——本次 [6] 找不到，仍然点了 `Send Data`。若当时列表中实际选中的是其他行，存在**烧错芯片**的产线风险。建议默认"失败即中止"（至少对控件/点击类步骤）。

---

## 二、修复方案：ListItem 匹配加固（P0）

### 改动点：`core/isp_controls.py` 的控件查找逻辑

**方案 2.1（推荐）— 纯数字标题改为"按行号定位"**
- 当控件标题是纯数字（如 `18`）且目标控件是 ListItem/行元素时，不再做标题字符串精确匹配，改为：枚举该列表控件全部行 → 取第 N 行（1-based）→ 用 `select()` 选中 + `click_input()` 点击该**行对象**。
- 优点：与行内容是否为空、是否刷新无关；`click_input` 作用于行对象本身会自动处理滚动到可见区域，顺带解决滚动问题。
- 兼容性：非纯数字标题维持现有精确匹配，不影响已录制的 Button/长标题 ListItem。

**方案 2.2（辅助）— 失败时增强诊断日志**
- 查找失败时，一次性 dump 该列表控件当前所有行的标题到日志（形如 `列表当前行: 1:'xxx' 2:'xxx' ...`）。
- 价值：下次再失败，日志直接给出"当时列表长什么样"，一眼看出是刷新、空格还是滚动导致。

**方案 2.3（可选）— 匹配放宽为"行号前缀匹配"**
- 标题以 `数字 + 空格` 开头即视为命中（如 `18 xxx` 也匹配录制时的 `18`）。
- 仅在 2.1 之外作为兜底，二选一即可。

**验证方式**：真机跑一次完整流程（Read I.S.P → LOAD → 点[18] → Send Data），观察日志中 dump 的行列表与点击结果。

### 改动点：`core/simulator.py` 失败处理策略（P0，产线安全）

- 新增配置项/开关：**"步骤失败即中止执行"**（默认开启）。
- 触发任一步骤失败时：立即停止、不执行 Send Data，summary 标记 `failed` 与失败步序号；日志明确提示"为防止烧错，已中止"。
- 如个别场景确需跳过继续，可在步骤编辑器按步骤勾选"失败后继续"（P2，可后做）。

---

## 三、弹窗监控方案：用弹窗判断烧录成败（P1）

### 截图弹窗特征

- 弹窗标题：**`COMPLETE`**，正文：**`Verify OK`**，按钮：**`确定`**（Win32 标准对话框，#32770）。
- 烧录过程中先出现 **`Meter`** 进度小窗（绿色进度条），完成后弹出 `COMPLETE`。
- 失败时预期同型弹窗，正文不同（如 `Verify NG` / 错误描述），需要真机采一次失败样本来确认文案。

### 设计：新增步骤类型 `waitresult`（等待烧录结果弹窗）

复用现有 `core/file_dialog.py` 的"等待-轮询-超时-可停止"骨架和 `core/isp_controls.py` 的窗口查找：

1. **触发时机**：一般紧跟 `Send Data` 步骤之后放置一个 `waitresult` 步骤。
2. **轮询逻辑**：
   - 在 ISP300 进程窗口下轮询标题为 `COMPLETE` 的对话框，间隔 0.5s，超时默认 120s（可在步骤里配置），轮询间检查停止信号（ESC 立即退出）。
   - 超时未出现 → 结果记为"未知/失败"（进度卡死或无弹窗都算不通过，宁严勿松）。
3. **结果判定**：读到弹窗后提取正文静态文本：
   - 含 `Verify OK` → 成功；
   - 含 `Verify NG` / `Error` / `Fail`（以真机采样为准）→ 失败；
   - 读不到正文 → 按失败处理。
4. **自动关闭**：判定完成后自动点击 `确定` 关闭弹窗，让流程可以循环跑下一片。
5. **结果落盘**：判定结果写入步骤 summary 与 UI（LED 红/绿 + 日志一行：`烧录结果: Verify OK`）。未来如需产线统计（良率/节拍），summary 结构已含 per-step 结果，可直接扩展导出 CSV（本期不做）。

### 备选方案（对比后不推荐做默认）

- **挂接 Send Data 自动等待**：点完 `Tx_start` 后引擎自动进入等待弹窗，无需新步骤类型。优点是用户零配置；缺点是"Through/Erase/All Erase"等其他操作也会弹 COMPLETE，语义不干净。可作为"步骤里加个勾选项"的简化形态，而不是独立逻辑。

### 边界与风险

| 风险 | 对策 |
|------|------|
| 失败弹窗文案未知（未采样） | 实施前请真机触发一次失败，截图/抓标题给我；判定规则按"白名单 OK，其余一律失败"设计，天然安全 |
| 弹窗被 Meter 进度窗遮挡 | 按**标题**匹配窗口而非坐标，不受遮挡影响 |
| 超时时间因芯片/文件大小而异 | 步骤内可配置超时（默认 120s） |
| 用户中途 ESC | 轮询循环检查 stop_event，立即退出并标记 stopped |
| 循环烧录多片场景 | waitresult 每轮独立判定并自动关窗，天然支持 |

---

## 四、实施顺序建议（确认后执行）

| 优先级 | 内容 | 改动文件 |
|--------|------|----------|
| P0 | ListItem 纯数字标题改行号定位 + 失败 dump 行列表 | `core/isp_controls.py` |
| P0 | 步骤失败即中止（防烧错） | `core/simulator.py` + UI 开关 |
| P1 | 新增 `waitresult` 步骤类型（弹窗监控判定烧录结果 + 自动关窗） | `core/constants.py`、`core/simulator.py`、`core/isp_controls.py`、`ui/step_editor.py` |
| P2 | 步骤级"失败后继续"勾选项（可选） | `ui/step_editor.py` |

### 需要你配合的两件事

1. **真机采样一次失败弹窗**（Verify NG 或报错时的标题/正文），用于确定失败判定关键词；
2. 确认"**失败即中止**"默认开启是否符合产线习惯（若有些工位允许跳过继续，请说明）。

> 本文档仅方案，未改动任何代码。确认后按上表顺序实施。
